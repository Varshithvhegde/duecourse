# Inception Labs (Mercury) — get a key at https://platform.inceptionlabs.ai (100M free tokens)
INCEPTION_API_KEY=
# Optional overrides — defaults shown
# LLM_BASE_URL=https://api.inceptionlabs.ai/v1
# LLM_MODEL=mercury-2

# Sanity Context MCP — org-level token with Context Viewer grant (NOT a project token)
SANITY_CONTEXT_MCP_URL=https://api.sanity.io/v1/context/organizations/<orgId>/mcp/<endpointName>
SANITY_CONTEXT_TOKEN=
